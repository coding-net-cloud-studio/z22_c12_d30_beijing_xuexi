# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 5000 # 应用的端口
    run: "pip install -r requirement.txt && python preview.py"
    root: ./ # 应用的启动目录
    name: 考研公告板 # 应用名称
    description: 考研公告板 # 应用描述
    autoOpen: true # 打开工作空间时是否自动开启预览(优先级高于根级 autoOpen)
